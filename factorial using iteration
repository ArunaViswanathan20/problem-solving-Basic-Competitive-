#include <stdio.h>
#include <stdlib.h>

int main()
{
    long long num,count,fact=1;
    printf("enter a num:");
    scanf("%d",&num);
    for(count=1;count<=num;count++)
    {
    	fact=fact*count;
	}
	printf("factorial is:%d",fact);
    return 0;
}
