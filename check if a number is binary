#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,d;
    printf("enter number:");
    scanf("%d",&n);
    if (n<0)
    {
	printf("non binary");
	return 0;
	}
	else{
	
    while(n>0)
    {
        d=n%10;
        if(d>=2)
        {
            printf("non binary");
            return 0;
        }
        n=n/10;
    }
	}
    printf("binary");
    return 0;
}
