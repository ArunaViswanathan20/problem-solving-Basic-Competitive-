#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    char s[789];
    int i;
    gets(s);
    for(i=0;i<strlen(s);i++)
    {
        if(s[i]>='a'&&s[i]<='z')
            s[i]=s[i]-32;
    }
    puts(s);
    return 0;
}
